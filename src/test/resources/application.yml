server:
  port: 0

spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:rectest;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE;LOCK_TIMEOUT=10000
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    open-in-view: false

app:
  jwt-secret: "test-secret-test-secret-test-secret-32b"
  admin-username: admin
  admin-password: admin123
